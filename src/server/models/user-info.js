
module.exports={
  /**
   * @description 验证用户登录信息
   * @author Michael Jian
   * @param {any} fromData 数据来源
   * @returns 返回验证结果与账套信息
   */
  async verifyUser(fromData){
    const result={};
    
    // 验证用户登录信息并获取位录账套的信息
    
    return result;
  },
}