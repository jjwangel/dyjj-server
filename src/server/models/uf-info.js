const mssql = require('./db-provide/mssql');

module.exports = {
  /**
   * @description 获取账套信息
   * @author Michael Jian
   * @returns 返回账套数据
   */
  async getUfAccountInfo() {
    let result = {
      retrun_code: '000000',
    };

    // 检索数据库返回账套数据。
    return result;
  },


}